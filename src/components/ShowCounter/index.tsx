import React from 'react'
import { useTableStore } from '../../utils/store'

export default function ShowCounter() {
  //   const maxEntries = useTableStore((state) => state.foundRows.length)
  return <p>Showing 1 of 2 entries</p>
}
